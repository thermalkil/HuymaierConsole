namespace HuymaierConsole.Modeling
{
    // Keep the GLB renderer source focused on glTF parsing while supplying the
    // two WPF Controls types it uses from the same compilation namespace.
    internal sealed class Viewport3D : System.Windows.Controls.Viewport3D { }
    internal sealed class Grid : System.Windows.Controls.Grid { }
}
