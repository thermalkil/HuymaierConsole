name: Validate Huymaier Console v0.26.5 Download Refresh

on:
  push:
    branches:
      - feature/v0.26.5-performance-and-downloads
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: huymaier-v0265-download-refresh-validation
  cancel-in-progress: true

jobs:
  validate:
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - name: Validate active-download library stability
        shell: powershell
        run: |
          $ErrorActionPreference='Stop'
          & '.\.development\v0.26.5\Test-v0265-DownloadRefreshPolicy.ps1'
