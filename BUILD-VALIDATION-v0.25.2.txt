Huymaier Console v0.25.2 build validation

Scope
- Focused stabilization over v0.25.1; no emulator-platform hub conversion.

Source validation targets
- 20 PowerShell source files passed lexical delimiter checks; 10 C# files passed lexical delimiter checks; 22 JSON files and 1 XML manifest parsed successfully.
- Dedicated Settings-owned Windows Update and Driver Update page definitions.
- Final-layer critical action routing for update pages and Quick Access chooser.
- Physical-mouse ownership guard on all MouseEnter navigation handlers.
- Seven-day / twenty-entry successful-install-only Downloads ledger plus provider-log backfill.
- Epic installed-manifest and direct-provider defensive filtering; library schema migration 4.
- PlayStation raw HID PS/Guide vs Options mapping.
- FSE package identity 0.25.2.0.
- Internal manifest version 0.25.2 / baseVersion 0.25.1.

Environment limitation
- This Linux build environment does not provide Windows PowerShell/.NET Framework WPF or csc.exe, so the final Windows native executable cannot be compiled or executed here. The supplied bootstrap performs PowerShell Parser preflight on the target PC and the installer performs its normal C# compilation. Treat v0.25.2 as a Windows test candidate until exercised on the target system.
